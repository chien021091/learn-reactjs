import React from 'react';
import PropTypes from 'prop-types';

Albums.propTypes = {
    
};

function Albums(props) {
    return (
        <div>
            Hello Album
        </div>
    );
}

export default Albums;